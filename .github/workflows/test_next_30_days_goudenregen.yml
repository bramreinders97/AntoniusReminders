name: Test next 30 days – Goudenregen

on:
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/test_next_30_days_template.yml
    with:
      CHAT_NAME: "Goudenregen"
    secrets:
      CHAT_ID: ${{ secrets.TEST_CHAT_ID_GOUDENREGEN }}
      BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
